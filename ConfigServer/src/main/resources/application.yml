server:
  port: 8087
spring:
  application:
    name: configServer
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mayur0502/microservice-configuration
          clone-on-start: true