server:
  port: 8083
#mongodb+srv://root:root@cluster0.pxu2fvz.mongodb.net/?appName=Cluster0
spring:
  data:
    mongodb:
#      uri: mongodb://localhost:27017/
#      database: microservices
      uri: mongodb+srv://root:root@cluster0.pxu2fvz.mongodb.net/?appName=Cluster0
      database: microservices
  application:
    name: RatingService
  config:
    import: optional:configserver:http://localhost:8087
eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka